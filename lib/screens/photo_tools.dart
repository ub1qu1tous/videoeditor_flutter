import 'package:flutter/material.dart';

class PhotoTools extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Photo Tools')),
      body: Center(child: Text('Welcome to Photo Tools')),
    );
  }
}