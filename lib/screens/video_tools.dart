import 'package:flutter/material.dart';

class VideoTools extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Video Tools')),
      body: Center(child: Text('Welcome to Video Tools')),
    );
  }
}